This directory contains the Godot GDExtension for RawPhotoForge.

Place the build output (libphoto_editor_godot.so) as follows:

```
.
├── README.txt
├── libs
│   └── Linux-x86_64
│       └── libphoto_editor_godot.so

```